import styled from '@emotion/styled';

export const ReturnButtonStyles = styled.button`
  position: fixed;
  top: 10px;
  left: 15px;
  background-color: var(--blue);
  color: #fff;
  border: none;
  border-radius: 5px;
  outline: none;

  padding: 4px 15px;
  z-index: 9;
  cursor: pointer;
`;
